# Broad-except audit (2026-06-02)

Drives Phase 2 of the error-handling/diagnostics plan
(docs/superpowers/plans/2026-06-02-error-handling-diagnostics.md).
Vendored copies under custom_components/foxess_control/smart_battery/
are excluded (synced from canonical smart_battery/).

Rows whose `file:line` lives under `smart_battery/` are brand-agnostic: the
fix lands in canonical `smart_battery/` and the pre-commit hook (C-015) syncs
it to the vendored copy under `custom_components/foxess_control/smart_battery/`.
The canonical module already ships a `_record_error(hass, domain, ...)` helper
(`smart_battery/listeners.py:222`); Phase 2 generalises it into the spec's
`record_error(...)` with hint/category/context and the always-on ring buffer.

| file:line | current handler | bucket | recommended action |
|---|---|---|---|
| custom_components/foxess_control/_services.py:126 | `_LOGGER.exception` then `raise` (catch-all after FoxESSApiError/RequestException/HAError already handled) | genuine-bug | Keep re-raise. `record_error` severity=error category=unexpected, context=service name; record to buffer. Anything reaching here is an unforeseen service-handler defect. |
| custom_components/foxess_control/foxess/web_session.py:201 | `async_discover_battery_id`: `_LOGGER.warning`, return None | config-or-environment | **Issue #8 site.** Narrow to `aiohttp.WSServerHandshakeError` (plus `aiohttp.ClientError`, `asyncio.TimeoutError`); `record_error` category=ws_discovery with host+plant_id context and region-mismatch hint; record to buffer. |
| custom_components/foxess_control/foxess/web_session.py:226 | BMS temp fetch: `_LOGGER.warning`, return None (broad catch after narrow FoxESSWebAuthError/Value/Type/Key) | config-or-environment | Narrow to `aiohttp.ClientError`/`asyncio.TimeoutError`; `record_error` category=bms_temperature with compound_id+host context, "check web credentials/region" hint; record to buffer. |
| custom_components/foxess_control/foxess/realtime_ws.py:326 | listen-loop receive error: `_LOGGER.debug`, reconnect | transient-retryable | Keep broad-ish; stays at debug; reconnect path self-heals. No buffer. |
| custom_components/foxess_control/foxess/realtime_ws.py:393 | `_on_data` callback raised: `_LOGGER.debug`, swallow | genuine-bug | A coordinator/inject callback raising is our defect — debug-swallow hides it. `record_error` severity=error category=ws_callback; record to buffer; do not silently swallow. |
| custom_components/foxess_control/foxess/realtime_ws.py:444 | reconnect attempt failed: `_LOGGER.debug`, invalidate token, retry with backoff | transient-retryable | Keep broad-ish at debug; designed retry-with-backoff loop self-heals. No buffer. |
| custom_components/foxess_control/_helpers.py:237 | `_write_export_limit`: `_LOGGER.warning`, no re-raise | config-or-environment | Narrow to HA service errors (`HomeAssistantError`, `ServiceNotFound`, `ValueError`); `record_error` category=export_limit_write with entity_id+value context, "check export-limit entity exists" hint; record to buffer. |
| smart_battery/listeners.py:295 (brand-agnostic) | `_create_unreachable_issue`: best-effort Repair create, `_LOGGER.debug` | intentional-suppression | Add one-line comment that Repair-registry create is non-critical; leave as-is. |
| smart_battery/listeners.py:305 (brand-agnostic) | `_clear_unreachable_issue`: best-effort Repair delete, `_LOGGER.debug` | intentional-suppression | Add one-line comment that Repair-registry delete is non-critical; leave as-is. |
| smart_battery/listeners.py:337 (brand-agnostic) | SoC read during circuit-breaker abort: fallback to None for log line | transient-retryable | Keep; the read is only for the abort log message. Drop to debug-level note; no buffer (the abort itself already calls `_record_error`). |
| smart_battery/listeners.py:356 (brand-agnostic) | `on_abort` cleanup also failed: `_LOGGER.exception` | genuine-bug | `record_error` severity=error category=abort_cleanup with session_type context; record to buffer. Cleanup failing during abort is a real defect/leak risk (C-025). |
| smart_battery/listeners.py:377 (brand-agnostic) | circuit-breaker inner_fn error counter: warn + count, opens breaker | transient-retryable | This IS the designed adapter-error retry mechanism (C-024); keep. It already calls `_record_error` when the breaker opens (line 400). Migrate that call to the new `record_error` category=adapter_error. |
| smart_battery/listeners.py:580 (brand-agnostic) | `_remove_charge_override`: `_LOGGER.warning`, schedule pending_override_cleanup retry | transient-retryable | Keep; pending-cleanup retry on next poll self-heals (network blip). Could log at info. No buffer (retry path handles it). |
| smart_battery/listeners.py:639 (brand-agnostic) | deferred-wake unsub() raised: `_LOGGER.debug` | intentional-suppression | Add one-line comment that cancelling a timer unsub is best-effort; leave as-is. |
| smart_battery/listeners.py:912 (brand-agnostic) | charge `adapter.on_session_started` raised: `_LOGGER.warning` | genuine-bug | A Protocol hook raising is an adapter defect. `record_error` severity=error category=adapter_hook with session_type=charge context; record to buffer. |
| smart_battery/listeners.py:1138 (brand-agnostic) | `_restore_export_limit`: `_LOGGER.warning`, no re-raise | config-or-environment | Narrow to the adapter/HA service errors; `record_error` category=export_limit_restore with entity context, C-025-cleanliness hint; record to buffer (leaving the HW cap modulated affects the next session — P-001). |
| smart_battery/listeners.py:1150 (brand-agnostic) | `_remove_discharge_override`: `_LOGGER.warning`, schedule pending retry | transient-retryable | Keep; mirror of :580, pending-cleanup retry self-heals. No buffer. |
| smart_battery/listeners.py:1340 (brand-agnostic) | discharge `adapter.on_session_started` raised: `_LOGGER.warning` | genuine-bug | Mirror of :912. `record_error` severity=error category=adapter_hook with session_type=discharge context; record to buffer. |
| smart_battery/listeners.py:1909 (brand-agnostic) | `_seed_export_limit`: `_LOGGER.warning`, no re-raise | config-or-environment | Mirror of :1138. Narrow to adapter/HA service errors; `record_error` category=export_limit_seed with entity context; record to buffer. |
| custom_components/foxess_control/sensor.py:828 | `RollingLogHandler.emit`: delegates to `self.handleError(record)` | intentional-suppression | Standard `logging.Handler.emit` contract — must not raise. Add one-line comment; leave as-is. |
| custom_components/foxess_control/sensor.py:925 | `InitDebugLogHandler.emit`: delegates to `self.handleError(record)` | intentional-suppression | Same as :828 (logging handler emit). Add one-line comment; leave as-is. |
| smart_battery/logging.py:61 (brand-agnostic) | `SessionContextFilter.filter`: on context_getter failure return record with empty ctx | intentional-suppression | A logging filter raising would break all logging. Add one-line comment; leave as-is. |
| smart_battery/sensor_base.py:956 (brand-agnostic) | best-effort sensor-write Repair create: `_LOGGER.debug` non-critical | intentional-suppression | Add one-line comment (Repair-registry create non-critical, may run during teardown); leave as-is. |
| smart_battery/sensor_base.py:974 (brand-agnostic) | best-effort sensor-write Repair clear: `_LOGGER.debug` non-critical | intentional-suppression | Add one-line comment; leave as-is. |
| custom_components/foxess_control/foxess_adapter.py:518 | export-limit entity write: `_LOGGER.warning` then `raise` | config-or-environment | Narrow to HA service errors; `record_error` category=export_limit_write with entity_id+value context, "entity missing/unavailable" hint; record to buffer; keep re-raise. |
| custom_components/foxess_control/foxess_adapter.py:729 | `apply_mode` work-mode select write: `_log_first_write(err)` then `raise` | config-or-environment | Narrow to HA service errors; `record_error` category=mode_write with work_mode_entity context; record to buffer; keep re-raise (mode entity missing/unavailable). |
| custom_components/foxess_control/foxess_adapter.py:754 | `apply_mode` power set_value write: `_log_first_write(err)` then `raise` | config-or-environment | Narrow to HA service errors; `record_error` category=power_write with power_entity+value context; record to buffer; keep re-raise. |
| custom_components/foxess_control/foxess_adapter.py:777 | `apply_mode` min-SoC set_value write: `_log_first_write(err)` then `raise` | config-or-environment | Narrow to HA service errors; `record_error` category=min_soc_write with min_soc_entity+value context; record to buffer; keep re-raise. |
| custom_components/foxess_control/foxess_adapter.py:816 | `set_export_limit_w` set_value write: `_log_first_write(err)` then `raise` | config-or-environment | Narrow to HA service errors; `record_error` category=export_limit_write with entity_id+value context; record to buffer; keep re-raise. |
| custom_components/foxess_control/coordinator.py:94 | `_get_capacity_kwh`: `_cfg` read fails, return 0.0 | config-or-environment | `_cfg` failing means config not loaded (env/lifecycle). Narrow to the `_cfg` lookup error (KeyError/AttributeError); `record_error` category=config_read with field=battery_capacity_kwh; record to buffer. Ambiguous vs genuine-bug — treated as config/env (conservative). |
| custom_components/foxess_control/coordinator.py:129 | `_bms_fetch_interval`: `_cfg` read fails, return default | config-or-environment | Same pattern as :94. Narrow to config-lookup error; `record_error` category=config_read field=bms_polling_interval; record to buffer. |
| custom_components/foxess_control/coordinator.py:143 | `_fetch_all`: `get_current_mode()` fails: `_LOGGER.debug`, set _work_mode=None | transient-retryable | Single API call inside the poll; self-heals next tick. Keep at debug; no buffer. |
| custom_components/foxess_control/coordinator.py:173 | `_retry_pending_cleanup`: `_cfg` read of min_soc_on_grid fails, default 11 | config-or-environment | Narrow to config-lookup error; `record_error` category=config_read field=min_soc_on_grid; record to buffer. |
| custom_components/foxess_control/coordinator.py:184 | pending override cleanup failed: `_LOGGER.warning`, retry next poll | transient-retryable | Keep; periodic retry self-heals (the retry path for :580/:1150). No buffer. |
| custom_components/foxess_control/coordinator.py:225 | BMS rediscovery `get_plant_id` failed: `_LOGGER.warning` exc_info, return None | config-or-environment | Narrow to API client + network errors; `record_error` category=plant_id_discovery with host context, region/auth hint; record to buffer. |
| custom_components/foxess_control/coordinator.py:250 | BMS rediscovery `async_discover_battery_id` failed: `_LOGGER.warning` exc_info, return None | config-or-environment | Narrow to `aiohttp` client/handshake + timeout; `record_error` category=ws_discovery with plant_id+host context, region-mismatch hint; record to buffer (same root cause as web_session.py:201 / issue #8). |
| custom_components/foxess_control/coordinator.py:287 | `_fetch_bms_temperature` failed: `_LOGGER.info` exc_info, preserve last temp | transient-retryable | Single web-portal fetch; preserves last value and self-heals next poll. Keep at info/debug; no buffer. |
| custom_components/foxess_control/coordinator.py:322 | `_async_update_data` REST poll (non-API-error path): keep last-known or `UpdateFailed` | transient-retryable | Single REST poll blip, keeps last-known data and re-raises only when no prior data. Keep; no buffer. |
| custom_components/foxess_control/__init__.py:438 | startup expired-schedule cleanup failed: `_LOGGER.exception` then del stored | config-or-environment | Narrow to API client/network errors; `record_error` category=schedule_cleanup with work_mode context; record to buffer (a failed clean-up may leave a stale override). |
| custom_components/foxess_control/__init__.py:871 | manifest.json read for cache-bust version: fallback "0" | intentional-suppression | Cache-buster only; failure has no functional impact. Add one-line comment; leave as-is. Ambiguous vs genuine-bug (a missing manifest in a shipped package would be a packaging defect) — treated as intentional-suppression because stakes are zero. |
| custom_components/foxess_control/__init__.py:905 | Lovelace resource auto-register failed: `_LOGGER.debug` "add manually" | intentional-suppression | Best-effort convenience with a documented manual fallback in the message. Add one-line comment; leave as-is. |
| custom_components/foxess_control/__init__.py:1055 | WS `get_plant_id` failed: `_LOGGER.warning` exc_info, fall back to REST | config-or-environment | Narrow to API client/network errors; `record_error` category=plant_id_discovery with host context, region/auth hint; record to buffer. |
| custom_components/foxess_control/__init__.py:1088 | `ws.async_connect()` failed: `_LOGGER.warning` exc_info, fall back to REST | config-or-environment | Narrow to `aiohttp.WSServerHandshakeError`/`ClientError`/timeout; `record_error` category=ws_connect with plant_id+host context, region-mismatch hint; record to buffer (same family as issue #8). |
| custom_components/foxess_control/__init__.py:1202 | integration version lookup failed: fallback "unknown" | intentional-suppression | Informational version string for the startup log only; no functional impact. Add one-line comment; leave as-is. Mild ambiguity vs genuine-bug — stakes zero, so intentional-suppression. |
| custom_components/foxess_control/__init__.py:1293 | taper profile load from Store failed: `_LOGGER.debug` exc_info, reset to empty | config-or-environment | Narrow to Store/JSON/deserialise errors; `record_error` category=storage_load field=taper_profile; record to buffer (corrupted persistent storage is an environment condition the user/maintainer should see — relates to C-014). |
| custom_components/foxess_control/__init__.py:1310 | `inverter.max_power_w` probe failed: raise `ConfigEntryNotReady` | config-or-environment | Already surfaces as `ConfigEntryNotReady("FoxESS Cloud unreachable")`. Narrow to API client/network errors; `record_error` category=cloud_reachability with host context, region/auth hint; record to buffer; keep the ConfigEntryNotReady re-raise. |
| custom_components/foxess_control/__init__.py:1381 | background battery-ID `get_plant_id` failed: `_LOGGER.debug` | config-or-environment | Narrow to API client/network errors; `record_error` category=plant_id_discovery with host context; record to buffer. |
| custom_components/foxess_control/__init__.py:1442 | periodic schedule-conflict check failed: bare `return` (NO log) | transient-retryable | Blind swallow with no log — add a `_LOGGER.debug` at minimum. Periodic 30-min poll self-heals, so keep broad-ish; no buffer. Flag: current code violates "no blind swallow" — add the debug log when migrating. |
| custom_components/foxess_control/__init__.py:1510 | session-replay probe `get_schedule` failed: `_LOGGER.debug`, retry next interval | transient-retryable | Expected-unreachable probe during circuit-breaker recovery; keep at debug. No buffer (success/expiry already logged at info). |
| custom_components/foxess_control/__init__.py:1555 | session-replay restart service call failed: `_LOGGER.exception` | genuine-bug | Restarting our own service should not fail; `_LOGGER.exception` already signals this. `record_error` severity=error category=session_replay with session_type context; record to buffer. Ambiguous vs config-or-environment (C-018 unmanaged-mode rejection is possible) — treated as genuine-bug (conservative: surface, never swallow). |

## Summary

- transient-retryable: 12
- config-or-environment: 21
- genuine-bug: 6
- intentional-suppression: 11
- Total unique production sites: 50
