{
  "services": {
    "clear_overrides": {
      "name": "Clear overrides",
      "description": "Return the inverter to self-use mode, clearing all or selected overrides.",
      "fields": {
        "mode": {
          "name": "Mode",
          "description": "Only clear overrides of this mode. If not set, all overrides are cleared."
        }
      }
    },
    "feedin": {
      "name": "Feed-in",
      "description": "Prioritise feeding excess solar to the grid for a set duration, optionally at a specific time and power limit.",
      "fields": {
        "duration": {
          "name": "Duration",
          "description": "How long to feed in (max 4 hours, must not cross midnight)."
        },
        "power": {
          "name": "Power limit",
          "description": "Feed-in power limit in watts. Defaults to inverter rated power if not set."
        },
        "start_time": {
          "name": "Start time",
          "description": "Time of day to start the override. Defaults to now if not set."
        },
        "replace_conflicts": {
          "name": "Remove conflicting overrides instead of aborting",
          "description": "If enabled, silently remove any existing overrides that overlap with the new time window instead of aborting."
        }
      }
    },
    "force_charge": {
      "name": "Force charge",
      "description": "Charge the battery for a set duration, optionally at a specific time and power limit.",
      "fields": {
        "duration": {
          "name": "Duration",
          "description": "How long to force charge (max 4 hours, must not cross midnight)."
        },
        "power": {
          "name": "Power limit",
          "description": "Charge power limit in watts. Defaults to inverter rated power if not set."
        },
        "start_time": {
          "name": "Start time",
          "description": "Time of day to start the override. Defaults to now if not set."
        },
        "replace_conflicts": {
          "name": "Remove conflicting overrides instead of aborting",
          "description": "If enabled, silently remove any existing overrides that overlap with the new time window instead of aborting."
        }
      }
    },
    "force_discharge": {
      "name": "Force discharge",
      "description": "Discharge the battery for a set duration, optionally at a specific time and power limit.",
      "fields": {
        "duration": {
          "name": "Duration",
          "description": "How long to force discharge (max 4 hours, must not cross midnight)."
        },
        "power": {
          "name": "Power limit",
          "description": "Discharge power limit in watts. Defaults to inverter rated power if not set."
        },
        "start_time": {
          "name": "Start time",
          "description": "Time of day to start the override. Defaults to now if not set."
        },
        "replace_conflicts": {
          "name": "Remove conflicting overrides instead of aborting",
          "description": "If enabled, silently remove any existing overrides that overlap with the new time window instead of aborting."
        }
      }
    },
    "smart_charge": {
      "name": "Smart charge",
      "description": "Charge the battery within a time window, periodically adjusting the charge rate to reach a target SoC by the end of the interval.",
      "fields": {
        "start_time": {
          "name": "Start time",
          "description": "Time of day to start charging."
        },
        "end_time": {
          "name": "End time",
          "description": "Time of day to stop charging. Must be after start time, within 4 hours."
        },
        "target_soc": {
          "name": "Target SoC",
          "description": "Charge the battery to this SoC level. Charging stops and reverts to self-use when reached."
        },
        "power": {
          "name": "Maximum power",
          "description": "Maximum charge power in watts. The actual power may be lower to pace charging to the end of the window."
        },
        "replace_conflicts": {
          "name": "Remove conflicting overrides instead of aborting",
          "description": "If enabled, silently remove any existing overrides that overlap with the new time window instead of aborting."
        }
      }
    },
    "smart_discharge": {
      "name": "Smart discharge",
      "description": "Discharge the battery within a time window, automatically reverting to self-use when a minimum SoC is reached.",
      "fields": {
        "start_time": {
          "name": "Start time",
          "description": "Time of day to start discharging."
        },
        "end_time": {
          "name": "End time",
          "description": "Time of day to stop discharging. Must be after start time, within 4 hours."
        },
        "power": {
          "name": "Power limit",
          "description": "Discharge power limit in watts. Defaults to inverter rated power if not set."
        },
        "min_soc": {
          "name": "Minimum SoC to stop discharge",
          "description": "Stop discharging and revert to self-use when the battery reaches this level."
        },
        "feedin_energy_limit_kwh": {
          "name": "Feed-in energy limit",
          "description": "Stop discharging after this much energy (kWh) has been fed into the grid, beyond household self-consumption."
        },
        "replace_conflicts": {
          "name": "Remove conflicting overrides instead of aborting",
          "description": "If enabled, silently remove any existing overrides that overlap with the new time window instead of aborting."
        }
      }
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "FoxESS Control",
        "description": "Enter your FoxESS Cloud API credentials.",
        "data": {
          "api_key": "API Key",
          "device_serial": "Device Serial Number"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to FoxESS Cloud API. Check your network connection.",
      "invalid_auth": "Authentication failed. Check your API key and device serial number."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "FoxESS Control Options",
        "data": {
          "min_soc_on_grid": "Minimum SoC on Grid (%)",
          "battery_capacity_kwh": "Battery Capacity (kWh)",
          "min_power_change": "Minimum Power Change for Smart Charge (W)",
          "api_min_soc": "Minimum API fdSoc (%) — ⚠️ Values below 11 may not work with all firmware versions",
          "polling_interval": "API Polling Interval (seconds)",
          "work_mode_entity": "Work mode entity (foxess_modbus select)",
          "charge_power_entity": "Charge power limit entity (foxess_modbus number)",
          "discharge_power_entity": "Discharge power limit entity (foxess_modbus number)",
          "min_soc_entity": "Min SoC entity (foxess_modbus number)",
          "soc_entity": "Battery SoC entity (sensor)",
          "loads_power_entity": "House load power entity (sensor)",
          "pv_power_entity": "Solar generation power entity (sensor)",
          "feedin_energy_entity": "Cumulative feed-in energy entity (sensor)"
        }
      }
    }
  }
}
